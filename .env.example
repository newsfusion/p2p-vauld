# Real platform credentials for smoke tests
# Copy this file to .env and fill in real values
# NEVER commit .env — it is in .gitignore

MINTOS_EMAIL=your_email@example.com
MINTOS_PASSWORD=your_password
